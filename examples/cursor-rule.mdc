---
description: AI memory kit pointer
alwaysApply: true
---

# Cursor rule example

See [Cursor rules](https://cursor.com/docs/rules). Do not overwrite an existing rule — add a pointer.

Read and follow `AGENTS.md` and `START.md`.
Shared memory: `MEMORY.md`, `memory/handoff.md`, `memory/open.md`.

If I already stated the goal, do not ask again.
Helpers: do not write memory files and do not /close.
